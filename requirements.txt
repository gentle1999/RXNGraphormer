# Legacy compatibility file.
#
# This branch is managed by pyproject.toml as a single rxngraphormer package.
#
# Do not install the old CUDA extension pin set from the original branch on
# Blackwell GPUs. Use:
#   uv sync
#
# For preprocessing, use envs/preprocess; it includes atom mapping and its
# torch ceiling is 2.4.0.
# CUDA wheels are selected by the install command.
